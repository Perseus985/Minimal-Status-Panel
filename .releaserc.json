{
  "branches": [
    "main",
    {
      "name": "develop",
      "prerelease": "beta"
    }
  ],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/changelog",
      {
        "changelogFile": "CHANGELOG.md"
      }
    ],
    [
      "@semantic-release/npm",
      {
        "npmPublish": false
      }
    ],
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "npm run build && cd dist && zip -r ../minimal-status-panel-${nextRelease.version}.zip . && cd .. && sha256sum minimal-status-panel-${nextRelease.version}.zip > checksums.txt"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [
          {
            "path": "minimal-status-panel-*.zip",
            "label": "Plugin Release (${nextRelease.version})"
          },
          {
            "path": "checksums.txt",
            "label": "Checksums"
          }
        ],
        "releaseBodyTemplate": "## Minimal Status Panel v${nextRelease.version}\n\n### 📦 Installation\n\n1. Download the `minimal-status-panel-${nextRelease.version}.zip` file\n2. Extract it to your Grafana plugins directory\n3. Restart Grafana\n4. Enable the plugin in your Grafana configuration\n\n### ✨ Features\n\n- Beautiful service status cards with clean design\n- Minimal and Full display modes\n- Interactive heartbeat bars with hover timestamps\n- Prometheus instance filtering\n- Real-time data integration\n- Custom service name mapping\n\n### 🛠️ Requirements\n\n- Grafana 9.0.0 or later\n- Prometheus with Blackbox Exporter (recommended)\n\n### 📋 Checksums\n\nSee the `checksums.txt` file for SHA256 checksums.\n\n---\n\n${nextRelease.notes}"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": [
          "CHANGELOG.md",
          "package.json",
          "package-lock.json"
        ],
        "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ]
  ]
}